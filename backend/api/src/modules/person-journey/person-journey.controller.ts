import { Body, Controller, Get, Param, Post, UseGuards } from '@nestjs/common'; import { CurrentOrganization } from '../../common/decorators/current-organization.decorator'; import { OrganizationContext } from '../../common/context/organization-context'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CreatePersonJourneyEventDto } from './dto/create-person-journey-event.dto'; import { PersonJourneyService } from './person-journey.service'; @Controller('person-journey-events') @UseGuards(JwtAuthGuard) export class PersonJourneyController { constructor(private readonly service: PersonJourneyService) {} @Post() create(@Body() dto: CreatePersonJourneyEventDto, @CurrentOrganization() context: OrganizationContext) { return this.service.create(dto, context); } @Get('person/:personId') find(@Param('personId') personId: string, @CurrentOrganization() context: OrganizationContext) { return this.service.findByPerson(personId, context); } }
