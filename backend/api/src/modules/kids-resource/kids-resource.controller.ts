import { Body, Controller, Get, Param, Patch, Post, UseGuards } from '@nestjs/common';
import { CurrentOrganization } from '../../common/decorators/current-organization.decorator'; import { OrganizationContext } from '../../common/context/organization-context'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CreateKidsResourceRequestDto } from './dto/create-kids-resource-request.dto'; import { CreateKidsVisualResourceDto } from './dto/create-kids-visual-resource.dto'; import { KidsResourceService } from './kids-resource.service';
@Controller('kids-resources') @UseGuards(JwtAuthGuard) export class KidsResourceController { constructor(private readonly s:KidsResourceService){} @Post() resource(@Body()d:CreateKidsVisualResourceDto,@CurrentOrganization()c:OrganizationContext){return this.s.createResource(d,c)} @Get() resources(@CurrentOrganization()c:OrganizationContext){return this.s.resources(c)} @Post('requests') request(@Body()d:CreateKidsResourceRequestDto,@CurrentOrganization()c:OrganizationContext){return this.s.request(d,c)} @Post('requests/:id/remind') remind(@Param('id')id:string,@CurrentOrganization()c:OrganizationContext){return this.s.remind(id,c)} @Get('events/:id/pending-returns') pending(@Param('id')id:string,@CurrentOrganization()c:OrganizationContext){return this.s.pending(id,c)} @Patch('requests/:id/:status') status(@Param('id')id:string,@Param('status')status:'READY'|'DELIVERED'|'RETURNED'|'CANCELLED',@CurrentOrganization()c:OrganizationContext){return this.s.status(id,status,c)} }
