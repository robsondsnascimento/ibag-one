import { Body, Controller, Post, UseGuards } from '@nestjs/common'; import { CurrentOrganization } from '../../common/decorators/current-organization.decorator'; import { OrganizationContext } from '../../common/context/organization-context'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { CellMultiplicationService } from './cell-multiplication.service'; import { CreateCellMultiplicationDto } from './dto/create-cell-multiplication.dto'; @Controller('cell-multiplications') @UseGuards(JwtAuthGuard) export class CellMultiplicationController { constructor(private readonly service: CellMultiplicationService) {} @Post() create(@Body() dto: CreateCellMultiplicationDto, @CurrentOrganization() context: OrganizationContext) { return this.service.create(dto, context); } }
